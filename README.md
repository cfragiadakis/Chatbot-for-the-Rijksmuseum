* pip install -r requirements.txt
* Append your .env file
* python data_extraction
* python xml_parser and select 2 for English version of Van Gogh's letters
* python build_chroma_db
* python questions_embeddings.py
* python .\question_answering.py 
* To run the webserver, use uvicorn app:app --reload
